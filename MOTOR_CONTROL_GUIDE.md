# คู่มือการใช้งาน BLEGS Actuator Unit

## 📋 สารบัญ
- [ภาพรวมระบบ](#ภาพรวมระบบ)
- [การเชื่อมต่อและตั้งค่า](#การเชื่อมต่อและตั้งค่า)
- [โหมดการทำงาน](#โหมดการทำงาน)
- [คำสั่งควบคุมมอเตอร์](#คำสั่งควบคุมมอเตอร์)
- [ข้อมูลที่มอเตอร์คืนค่า](#ข้อมูลที่มอเตอร์คืนค่า)
- [สถานะ LED](#สถานะ-led)
- [ตัวอย่างการใช้งาน](#ตัวอย่างการใช้งาน)
- [การแก้ปัญหา](#การแก้ปัญหา)

---

## ภาพรวมระบบ

**BLEGS Actuator Unit** เป็นระบบควบคุมมอเตอร์ BLDC แบบ Field-Oriented Control (FOC) พร้อมระบบควบคุมตำแหน่งที่แม่นยำ

### ข้อมูลฮาร์ดแวร์
- **MCU**: STM32G431CBU6 (170MHz ARM Cortex-M4)
- **RAM**: 32KB
- **Flash**: 128KB
- **Encoder**: AS5047P (14-bit Magnetic Encoder)
- **Gear Ratio**: 8:1
- **PWM Frequency**: 20kHz (SVPWM)

### ความสามารถของระบบ
- ✅ ควบคุมตำแหน่งแบบ Position Control
- ✅ ควบคุมตำแหน่งด้วย S-Curve Profile (เคลื่อนที่นุ่มนวล)
- ✅ Multi-turn Tracking (ติดตามรอบหมุน)
- ✅ EEPROM สำหรับบันทึกค่า Calibration
- ✅ สถานะ LED แสดงผล 16 สี

---

## การเชื่อมต่อและตั้งค่า

### 1. การเชื่อมต่อ Serial Port
- **Baud Rate**: 921,600 bps
- **Data Format**: 8-N-1 (8 data bits, No parity, 1 stop bit)
- **Port Type**: System Serial (USB หรือ RS232)

### 2. การเริ่มต้นระบบ

เมื่อเปิดเครื่อง ระบบจะทำการ:
1. **Initialization** - เริ่มต้น Serial, LED, EEPROM, Encoder
2. **Motor Alignment** - หาค่า offset ของมอเตอร์
3. **Calibration Check** - ตรวจสอบมุมปัจจุบัน
4. **Ready State** - รอกดปุ่ม START เพื่อเริ่มทำงาน

### ข้อมูลแสดงผลขณะ Calibration Check
```
Turns: <จำนวนรอบ>    Angle w/o offset: <มุมไม่มี offset>    Offset: <ค่า offset>    Angle w offset: <มุมที่มี offset>
```

---

## โหมดการทำงาน

ระบบมี 2 โหมดควบคุมตำแหน่ง:

### โหมด 0: Position Control Only
- **ลักษณะ**: ควบคุมตำแหน่งโดยตรง ไม่มี motion profile
- **ความเร็ว**: ตอบสนองเร็ว แต่อาจกระตุก
- **เหมาะสำหรับ**: การทดสอบ, การเคลื่อนที่ระยะสั้น

### โหมด 1: Position Control with S-Curve (Default)
- **ลักษณะ**: ควบคุมตำแหน่งด้วย S-Curve Motion Profile
- **ความเร็ว**: นุ่มนวล ไม่กระตุก
- **พารามิเตอร์**:
  - Maximum Velocity: 4,000 deg/s
  - Maximum Acceleration: 180,000 deg/s²
  - Jerk Time: 1,000 ms
- **เหมาะสำหรับ**: การใช้งานจริง, การเคลื่อนที่แม่นยำ

---

## คำสั่งควบคุมมอเตอร์

### 1. เปลี่ยนโหมดการทำงาน

#### คำสั่ง: `M<mode_number>`

**พารามิเตอร์:**
- `mode_number`: หมายเลขโหมด (0 หรือ 1)

**ตัวอย่าง:**
```
M0    → เปลี่ยนเป็นโหมด Position Control Only
M1    → เปลี่ยนเป็นโหมด Position Control with S-Curve
```

**คำตอบจากระบบ:**
```
Mode: Position Control Only
หรือ
Mode: Position Control with S-Curve
```

---

### 2. ตั้งค่าตำแหน่งเป้าหมาย

#### คำสั่ง: `#<position>`

**พารามิเตอร์:**
- `position`: ตำแหน่งเป้าหมาย (องศา)
  - หน่วย: degrees
  - ช่วงค่า: -∞ ถึง +∞ (รองรับ multi-turn)
  - ทศนิยม: รองรับ (เช่น 45.5, -90.25)

**พฤติกรรมตามโหมด:**

**โหมด 0 (Position Control Only):**
- ตั้งค่าตำแหน่งโดยตรง ไม่มี motion planning
- มอเตอร์จะเคลื่อนที่ไปที่ตำแหน่งเป้าหมายอย่างรวดเร็ว

**โหมด 1 (Position Control with S-Curve):**
- สร้าง S-Curve motion profile จากตำแหน่งปัจจุบันไปยังเป้าหมาย
- มอเตอร์จะเคลื่อนที่แบบนุ่มนวล มีการเร่ง-ชะลอที่ควบคุม

**ตัวอย่าง:**
```
#90.0     → ไปยังตำแหน่ง 90 องศา
#-45.5    → ไปยังตำแหน่ง -45.5 องศา
#0        → กลับตำแหน่ง 0 องศา
#720      → หมุน 2 รอบ (720 องศา)
```

**คำตอบจากระบบ:**
```
Direct setpoint: <position>        (โหมด 0)
หรือ
S-Curve setpoint: <position>       (โหมด 1)
```

---

## ข้อมูลที่มอเตอร์คืนค่า

### รูปแบบข้อมูล Real-time Feedback

ระบบจะส่งข้อมูล feedback กลับมาเป็นระยะ (ตามค่า `DEBUG_PERIOD_US`):

```
Returns:    <setpoint>    <current_position>
```

### คำอธิบายข้อมูล

| ฟิลด์ | คำอธิบาย | หน่วย | ค่าทศนิยม |
|-------|----------|-------|-----------|
| **setpoint** | ตำแหน่งเป้าหมายปัจจุบัน (Target Position) | องศา (degrees) | 1 ตำแหน่ง |
| **current_position** | ตำแหน่งจริงของมอเตอร์ (Actual Position) | องศา (degrees) | 1 ตำแหน่ง |

### ตัวอย่างข้อมูลที่ได้รับ

**กรณี: กำลังเคลื่อนที่ไปยัง 90 องศา**
```
Returns:    45.2    44.8
Returns:    52.8    52.3
Returns:    63.5    63.2
Returns:    78.1    78.0
Returns:    88.9    88.8
Returns:    90.0    90.0
```

**การตีความ:**
- `setpoint` เพิ่มขึ้นเรื่อยๆ (ในโหมด S-Curve) หรือคงที่ (ในโหมด Direct)
- `current_position` ตามหลัง setpoint ด้วย error เล็กน้อย
- เมื่อถึงเป้าหมาย: `setpoint` ≈ `current_position`

---

## สถานะ LED

ระบบใช้ LED NeoPixel 1 ดวงแสดงสถานะ:

### สถานะการทำงาน

| สถานะ | สี | รูปแบบ | ความหมาย |
|-------|-----|---------|----------|
| **INIT** | 🟡 เหลือง | สว่างคงที่ | กำลังเริ่มต้นระบบ |
| **READY** | 🟢 เขียว | สว่างคงที่ | พร้อมทำงาน (มุมอยู่ในช่วง calibration ±20°) |
| **RUNNING** | 🔵 น้ำเงิน | สว่างคงที่ | กำลังทำงาน (หลังกดปุ่ม START) |
| **ERROR** | 🔴 แดง | กระพริบ | เกิดข้อผิดพลาด |
| **WARNING** | 🟠 ส้ม | สว่างคงที่ | คำเตือน |
| **CALIBRATING** | 🟡 เหลือง | หายใจ (breathing) | กำลัง calibrate |
| **COMMUNICATION** | 🔷 ฟ้า | กระพริบ 5 ครั้ง | กำลังสื่อสาร |
| **STANDBY** | 🟢 เขียว | หายใจช้า | โหมดพักงาน |

### การตรวจสอบสถานะตามสี LED

**ก่อนกดปุ่ม START:**
- 🟢 **เขียว** = มุมปัจจุบันอยู่ในช่วงที่ถูกต้อง (±20° จาก -90°×GEAR_RATIO)
- 🟡 **เหลือง/แดง** = มุมอยู่นอกช่วง ต้องปรับตำแหน่งก่อน

**หลังกดปุ่ม START:**
- 🔵 **น้ำเงิน** = ระบบทำงานปกติ

---

## ตัวอย่างการใช้งาน

### ตัวอย่างที่ 1: การควบคุมตำแหน่งแบบ Direct

```
> M0
Mode: Position Control Only

> #0
Direct setpoint: 0.0

> #90
Direct setpoint: 90.0

> #-45
Direct setpoint: -45.0
```

**ผลลัพธ์:**
- มอเตอร์จะเคลื่อนที่ไปยังตำแหน่งที่กำหนดอย่างรวดเร็ว
- อาจมีการกระตุก (jerk) เล็กน้อย

---

### ตัวอย่างที่ 2: การควบคุมตำแหน่งด้วย S-Curve

```
> M1
Mode: Position Control with S-Curve

> #180
S-Curve setpoint: 180.0

Returns:    45.2    44.9
Returns:    87.5    87.1
Returns:    132.8   132.5
Returns:    170.3   170.1
Returns:    180.0   180.0
```

**ผลลัพธ์:**
- มอเตอร์จะเคลื่อนที่แบบนุ่มนวล ไม่กระตุก
- `setpoint` จะค่อยๆ เพิ่มขึ้นตาม S-Curve profile
- `current_position` จะติดตาม `setpoint` อย่างแม่นยำ

---

### ตัวอย่างที่ 3: การหมุนหลายรอบ (Multi-turn)

```
> M1
Mode: Position Control with S-Curve

> #720
S-Curve setpoint: 720.0

Returns:    180.5   180.2
Returns:    360.8   360.4
Returns:    540.2   539.9
Returns:    720.0   720.0
```

**ผลลัพธ์:**
- มอเตอร์จะหมุน 2 รอบเต็ม (720° / 360° = 2 รอบ)
- ระบบติดตามจำนวนรอบอัตโนมัติ

---

### ตัวอย่างที่ 4: การสลับโหมดระหว่างการทำงาน

```
> M1
Mode: Position Control with S-Curve

> #90
S-Curve setpoint: 90.0

(รอให้มอเตอร์เคลื่อนที่ถึงตำแหน่ง)

> M0
Mode: Position Control Only

> #120
Direct setpoint: 120.0

(มอเตอร์จะเคลื่อนที่แบบ direct ไปที่ 120°)
```

**ผลลัพธ์:**
- สามารถสลับโหมดได้ตลอดเวลา
- การเคลื่อนที่ครั้งถัดไปจะใช้โหมดที่เลือกล่าสุด

---

## การแก้ปัญหา

### ปัญหา: มอเตอร์ไม่เคลื่อนที่

**สาเหตุที่เป็นไปได้:**
1. ❌ ยังไม่กดปุ่ม START
   - **แก้ไข**: กดปุ่ม START บนบอร์ด

2. ❌ ตำแหน่งเป้าหมายเท่ากับตำแหน่งปัจจุบัน
   - **แก้ไข**: ตั้งค่าตำแหน่งใหม่ที่แตกต่างจากปัจจุบัน

3. ❌ มอเตอร์ค้างอยู่ที่ตำแหน่งเดิม
   - **แก้ไข**: Reset บอร์ดและลองใหม่

---

### ปัญหา: มอเตอร์กระตุกหรือสั่น

**สาเหตุและวิธีแก้:**
1. ⚠️ ใช้โหมด Position Control Only
   - **แก้ไข**: เปลี่ยนเป็นโหมด S-Curve (`M1`)

2. ⚠️ ค่า PID ไม่เหมาะสม
   - **แก้ไข**: ปรับค่า PID ในไฟล์ `motor_control.h`

3. ⚠️ ตำแหน่งเป้าหมายเปลี่ยนบ่อยเกินไป
   - **แก้ไข**: รอให้มอเตอร์ถึงตำแหน่งก่อนส่งคำสั่งใหม่

---

### ปัญหา: ค่า Position ไม่ตรงกับที่ตั้ง

**สาเหตุและวิธีแก้:**
1. ⚠️ ยังไม่ได้ Calibrate มอเตอร์
   - **แก้ไข**: ทำการ calibrate ตามขั้นตอนใน setup()

2. ⚠️ ค่า GEAR_RATIO ไม่ถูกต้อง
   - **แก้ไข**: ตรวจสอบและแก้ไขใน `motor_conf.h`

3. ⚠️ Encoder เสีย
   - **แก้ไข**: ตรวจสอบการเชื่อมต่อ SPI และ encoder

---

### ปัญหา: LED แสดงสีแดงตลอดเวลา

**สาเหตุและวิธีแก้:**
1. ⚠️ มุมปัจจุบันอยู่นอกช่วง calibration (±20°)
   - **แก้ไข**: หมุนมอเตอร์ด้วยมือให้อยู่ในตำแหน่งที่ถูกต้อง (~-720° สำหรับ calibration_angle = -90°)

2. ❌ ระบบเกิด error
   - **แก้ไข**: Reset บอร์ดและตรวจสอบ Serial output

---

### ปัญหา: ไม่มีข้อมูล Returns แสดง

**สาเหตุและวิธีแก้:**
1. ❌ Serial port ไม่ได้เชื่อมต่อ
   - **แก้ไข**: ตรวจสอบการเชื่อมต่อ USB/RS232

2. ❌ Baud rate ไม่ถูกต้อง
   - **แก้ไข**: ตั้ง Baud rate เป็น 921,600

3. ❌ ยังไม่ได้กดปุ่ม START
   - **แก้ไข**: กดปุ่ม START เพื่อเริ่มทำงาน

---

## ข้อมูลเพิ่มเติม

### ไฟล์ Configuration หลัก

| ไฟล์ | คำอธิบาย |
|------|----------|
| `motor_conf.h` | ค่า config ของมอเตอร์ (GEAR_RATIO, pole pairs, etc.) |
| `motor_control.h` | ค่า PID และ control parameters |
| `system.h` | ค่า Serial baud rate และ pin definitions |
| `led.h` | การตั้งค่า LED และสี preset |

### ค่า Configuration ที่สำคัญ

```c
// motor_conf.h
#define GEAR_RATIO                  8.0f            // เกียร์ทด 8:1
#define POLE_PAIRS                  7               // จำนวนขั้วแม่เหล็ก

// motor_control.h
#define POSITION_CONTROL_PERIOD     0.001f          // 1ms (1kHz)
#define DEBUG_PERIOD                0.05f           // 50ms (20Hz)

// system.h
#define SERIAL1_BAUD_RATE           921600          // Baud rate สำหรับ System Serial
```

### S-Curve Parameters (ปรับได้ในโค้ด)

```c
scurve.plan(
    current_pos,        // ตำแหน่งเริ่มต้น
    new_setpoint,       // ตำแหน่งเป้าหมาย
    4000.0f,           // Max Velocity (deg/s)
    180000.0f,         // Max Acceleration (deg/s²)
    1000.0f,           // Jerk time (ms)
    180000.0f          // Max Deceleration (deg/s²)
);
```

---

## สรุป

### คำสั่งที่ใช้บ่อย

| คำสั่ง | ความหมาย | ตัวอย่าง |
|--------|----------|----------|
| `M0` | เปลี่ยนเป็นโหมด Direct | `M0` |
| `M1` | เปลี่ยนเป็นโหมด S-Curve | `M1` |
| `#<pos>` | ตั้งตำแหน่งเป้าหมาย | `#90.5` |

### ข้อมูลที่ได้รับ

```
Returns:    <setpoint>    <current_position>
```

### LED Status

- 🟢 เขียว = พร้อมใช้งาน / Standby
- 🔵 น้ำเงิน = กำลังทำงาน
- 🟡 เหลือง = กำลังเริ่มต้น / Calibrating
- 🔴 แดง = ข้อผิดพลาด / ตำแหน่งผิด

---

**เอกสารนี้ปรับปรุงล่าสุด: พฤศจิกายน 2025**
