# Action Plan — BLEGS TCI Tier 1 paper

อัปเดต: 6 สิงหาคม 2026

## สถานะปัจจุบัน

| ส่วน | สถานะ |
|---|---|
| บทที่ 1 บทนำ | ยังไม่เขียน (ไฟล์ว่าง) |
| บทที่ 2 วิธีการวิจัย | **§2.1–2.3 เขียนครบแล้ว** มี 26 สมการ · §2.3.3 เป็นการออกแบบรอทดสอบ |
| บทที่ 3 ผลการทดลอง | ยังไม่เขียน (ไฟล์ว่าง) — ข้อมูล §2.3.2 พร้อมแล้ว, §2.2 ต้องเคลียร์ปัญหาก่อน |
| บทที่ 4 อภิปรายและสรุป | ยังไม่เขียน (ไฟล์ว่าง) |
| รูปภาพ | ยังไม่มี — บทที่ 2 มี placeholder ไว้ 9 รูป |
| เอกสารอ้างอิง | ยังไม่มี |

**การทดลองที่มีข้อมูลแล้ว:** §2.1, §2.2 (ทุกส่วน), §2.3.2 (เงื่อนไขละ 1 รอบ)
**การทดลองที่ยังไม่ได้ทำ:** §2.3.3 ทั้งหมด

---

## ลำดับที่ 1 — เคลียร์ปัญหาที่กระทบตัวเลขที่จะตีพิมพ์

ทุกข้อตรวจสอบกับไฟล์จริงแล้ว ต้องตัดสินใจก่อนเขียนบทที่ 3

### 1.1 รัศมีวงกลมอ้างอิงใน §2.2.5 ไม่ตรงกัน — **กระทบมากที่สุด**

เปเปอร์ระบุ r = 35 mm แต่สคริปต์วิเคราะห์ทั้งสองตัวใช้ 30 mm และจุดศูนย์กลางไม่ตรงกันเอง

| ไฟล์ | ศูนย์กลาง Y | รัศมี |
|---|---|---|
| `vision_based_trajectory_eval.py:34-35` | −170 mm | 30 mm |
| `calibration_coverage_check.py:64-65` | −175 mm | 30 mm |
| เปเปอร์ §2.2.5 | — | 35 mm |

ความคลาดเคลื่อนเชิงรัศมีคำนวณจากค่านี้โดยตรง ($e_i = \sqrt{\cdot} - r$) ผิด 5 mm บนผลที่รายงานระดับ ~1 mm

**ต้องทำ:** ยืนยันว่าตอนถ่ายวิดีโอสั่งวงกลมรัศมีเท่าไรจริง (ดูจาก log หรือคำสั่ง `circle` ที่ใช้) แล้วแก้ให้ตรงกันทั้งสามที่

### 1.2 จำนวนจุดกริดใน §2.2.3

เปเปอร์เขียน 180 จุด / 720 ชุดข้อมูล แต่ไฟล์จริงมี **152 จุด / 608 แถว** (`workspace_grid.csv`, `grid_log.csv`)

**ต้องทำ:** แก้ตัวเลขในเปเปอร์เป็น 152/608 (ไม่กระทบผล) หรือเก็บข้อมูลเพิ่มถ้าต้องการ 180 จริง

### 1.3 ข้อมูล §2.2 เก็บมาโดยไม่ได้แก้ความผิดเพี้ยนเลนส์

`single_leg_xy_control.py:104` ชี้ไป `calibration_olympus25mm.npz` ซึ่งไม่มีอยู่จริง (ไฟล์จริงคือ `output/params/calibration_olympus25f1.2.npz`) ตัวโหลดคืนค่า None เงียบๆ แล้วรันต่อ ขัดกับที่ §2.2.1 เขียนไว้ว่าชดเชย lens distortion แล้ว `video_grid_analysis.py:39` ผิดแบบเดียวกัน

**ต้องทำ:** แก้ path แล้วประมวลผลวิดีโอเดิมซ้ำ (ไม่ต้องถ่ายใหม่) เพื่อดูว่าค่าเปลี่ยนมากแค่ไหน ถ้าเปลี่ยนน้อยให้ระบุเป็นข้อจำกัด ถ้าเปลี่ยนมากต้องเทรนโมเดลใหม่

### 1.4 ตาราง RMSE ของโมเดลเป็นค่าในชุดฝึก ไม่ใช่ค่า generalization

`train_compensation_model.py:25-28` `fit` แล้ว `predict` บนชุดเดียวกัน ดังนั้น `training_summary.txt` เป็น in-sample ทั้งหมด ขณะที่ §2.2.4 เขียนว่าใช้ cross-condition validation (ฝึกด้วย static → ประเมินด้วย dynamic)

**ต้องทำ:** ห้ามนำตารางนั้นไปเป็น "ความแม่นยำของแบบจำลอง" ในบทที่ 3 ให้คำนวณ RMSE จากข้อมูล dynamic (§2.2.5) ตามที่เปเปอร์อธิบายไว้จริง และเลือกโมเดลจากค่านั้น หมายเหตุ: การเลือกจากค่า in-sample จะได้ Random Forest ซึ่งเป็นตัวที่ overfit ที่สุด ขณะที่ระบบจริงตั้ง `model_poly4` เป็นค่าเริ่มต้น

### 1.5 นิยามแขนโมเมนต์ใน §2.1

เขียนว่า r = 0.105 m "วัดจากแกนหมุนของข้อต่อถึงตำแหน่งปลายขา" แต่ 105 mm คือความยาวก้านมอเตอร์ ($L_{AC}$) ส่วนปลายขาอยู่ไกลกว่านั้น (มี $L_{CE}$ = 145 mm ต่ออีกทอด) กระทบค่า torque-to-weight ซึ่งเป็นตัวเลขหลักของเปเปอร์

**ต้องทำ:** ยืนยันว่าเครื่องวัดแรงต่อที่จุดใด แล้วแก้ค่า r หรือแก้คำอธิบายให้ตรง

---

## ลำดับที่ 2 — เขียนบทที่ 3 (ผลการทดลอง)

### 2.1 §2.3.2 พร้อมเขียนแล้ว

ข้อมูลครบทั้ง 4 เงื่อนไข ผ่านการตรวจสอบไขว้แล้ว (วัดซ้ำด้วยการสุ่มเฟรมต่างกัน 4 แบบ ได้ค่าตรงกันภายใน 1%)

ต้องรายงานประกอบ: ค่าอัตราขยายพารัลแลกซ์ $m$ ของแต่ละรอบ, สัดส่วนข้อมูลที่อยู่นอกกริดอ้างอิง, และจำนวนรอบต่อเงื่อนไข (1 รอบ)

**ข้อควรระวังในการตีความ:** สองรอบที่ปิดการชดเชยเบี่ยงไปคนละทิศทาง ด้วยข้อมูลเงื่อนไขละ 1 รอบยังแยกไม่ได้ว่าเป็นคุณสมบัติของหุ่นหรือผลจากการวางตำแหน่งเริ่มต้น ควรอภิปรายเป็นข้อจำกัด ไม่ใช่ข้อค้นพบ

### 2.2 §2.2 ต้องรอข้อ 1.1–1.4 ก่อน

### 2.3 §2.1 ต้องรอข้อ 1.5 ก่อน

---

## ลำดับที่ 3 — ทำการทดลอง §2.3.3

ออกแบบไว้ครบแล้วในบทที่ 2 ก่อนลงมือต้องเตรียม:

**ค่าที่ยังไม่กำหนด** (มี `<mark>` กำกับไว้ในเนื้อหา): ระยะทางทดสอบ $L$, ขั้นการเพิ่มมุมความชันและช่วงที่ทดสอบ, สเปกเชิงกายภาพของพื้นผิวยืดหยุ่นและพื้นผิวขรุขระ

**งานฝั่งระบบวัดที่ต้องทำก่อน:**

1. **เพิ่มอัตราบันทึกกระแสเป็น 50 Hz** ปัจจุบัน `LOG_RATE = 10` ทำให้ได้แค่ 5 Hz ซึ่งไม่พอสำหรับอินทิเกรตพลังงาน (รอบก้าวอยู่ที่ ~1.8 Hz จะได้แค่ ~3 จุดต่อรอบ ยอดกระแสช่วงถีบพื้นจะหลุด)
2. **สอบเทียบสัมประสิทธิ์ $\eta$** ในสมการที่ (25) ชุดขับเคลื่อนรายงานกระแสเฟส ไม่ใช่กระแสบัส และเพราะ PID ถูกจำกัดที่ ±12 V บนบัส 24 V ดิวตี้จึงไม่เกิน ~50% ถ้าเอากระแสเฟสคูณแรงดันบัสตรงๆ CoT จะสูงเกินจริงเกือบเท่าตัว — วัดกระแสบัสจริงด้วยแคลมป์มิเตอร์เทียบสักจุด
3. **ตัดสินใจเรื่อง $V_{bus}$** เฟิร์มแวร์ไม่รายงานแรงดันบัส จะสมมติ 24 V คงที่แล้วระบุเป็นข้อจำกัด หรือวัดจากภายนอก
4. **ทดสอบโหมด teleop บนแท่นก่อนลงพื้น** โดยเฉพาะการหมุนอยู่กับที่ที่ค่า k สูง (ขาฝั่งในก้าวถอยหลัง ต้องดูอาการเท้าครูด)

---

## ลำดับที่ 4 — งานที่เหลือของต้นฉบับ

- เขียนบทที่ 1 บทนำ และบทที่ 4 อภิปราย/สรุป
- ทำรูปทั้ง 9 รูปตาม placeholder ในบทที่ 2 (ข้อความในรูปต้องเป็นภาษาอังกฤษ อ่านได้เมื่อพิมพ์ขาวดำ กว้างไม่เกิน 7.5 หรือ 15.5 ซม.)
- รวบรวมเอกสารอ้างอิงรูปแบบ IEEE
- แปลงเป็น Word ตาม `01_Manuscript/FORMAT_GUIDE.md` (TH Sarabun New, 2 คอลัมน์, 8–10 หน้า) — ต้องระวังว่าเนื้อหาปัจจุบันยาวเกิน 10 หน้าหรือไม่
- คัดลอกสคริปต์เวอร์ชันล่าสุดจากรีโปโค้ดมาทับ `04_Code/` ตอนส่งบทความ (ตอนนี้เป็นเวอร์ชันเก่าแล้ว)

---

## ข้อเสนอสำหรับการเก็บข้อมูลรอบถัดไป (ถ้ามี)

- **ยกป้ายอ้างอิงทางวิ่งขึ้นให้อยู่ระดับเดียวกับป้ายบนหลังหุ่น (~31 cm)** จะตัดพารัลแลกซ์ทิ้งตั้งแต่ต้นทาง ไม่ต้องแก้ค่าย้อนหลัง
- **ขยายกริดเครื่องหมายให้คลุมระยะเดินจริง** ปัจจุบันคลุม 120 cm แต่หุ่นเดิน ~2 m ทำให้ครึ่งหนึ่งของข้อมูลเป็นการประมาณค่านอกช่วง
- **เก็บเงื่อนไขละ 5 รอบ** ตามที่ §2.2.5 และ §2.1 ทำ เพื่อให้รายงานค่าเฉลี่ยและส่วนเบี่ยงเบนมาตรฐานได้
- พิมพ์ป้าย ID 7 ใหม่แทนป้าย ID 6 ที่ซ้ำอยู่บนทางวิ่ง
